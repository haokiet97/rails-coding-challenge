
= form_for(@note) do |f|
  = render "validation_errors", record: @note
  
  .form-group
    = f.text_area :text, rows: 5, class: "form-control"

  .form-actions
    = f.submit :class => "btn btn-primary"
    = link_to "Cancel", contact_path(@note.subject), :class => "btn"
